/**
 * Common application constants
 */

// Pagination constants
export const PAGINATION = {
    ARTICLES_PER_PAGE: 20,
    DEFAULT_PAGE: 1,
    USERS_SEARCH_LIMIT: 10,
    TAGS_INITIAL_LIMIT: 8,
} as const;

// API defaults
export const API_DEFAULTS = {
    DEFAULT_LIMIT: 10,
} as const;
